{
  "name": "daft",
  "version": "1.0.0",
  "description": "Declarative Agents, Fast & Type-safe",
  "type": "module",
  "scripts": {
    "submit": "bun src/cli/submit.ts",
    "worker": "bun src/cli/worker.ts",
    "logs": "bun src/cli/logs.ts",
    "run-local": "bun src/cli/run-local.ts",
    "cleanup": "bun src/cli/cleanup-workers.ts",
    "docs:build": "typedoc src/lib/types.ts src/lib/runtime.ts src/tools/index.ts src/tools/predicates.ts --out docs/api --name DAFT --readme README.md --hideGenerator --skipErrorChecking",
    "docs:serve": "bun run docs:build && npx serve docs/api"
  },
  "dependencies": {
    "redis": "^5.11.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typedoc": "^0.28.15"
  }
}
